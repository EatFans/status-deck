# Status Deck

Status Deck 是一个基于 ESP32 的开源桌面状态监控卡。它通过 BLE 与电脑端 Agent 通信，将 AI 用量、API 额度、服务器状态、系统运行数据等实时显示在桌面小屏上。

这个项目的目标是做一个低成本、可复刻、可扩展的实体状态面板：不用频繁切换窗口，也能一眼看到当前工作流和基础设施的健康状态。

## 功能规划

- ESP32 设备端通过 BLE 接收电脑端 Agent 推送的数据
- 实时显示 AI 用量、API 额度、账户余量或调用统计
- 显示服务器状态、延迟、CPU、内存、磁盘、服务可用性等指标
- 支持本机系统状态，例如电量、网络、CPU、内存、正在运行的任务
- 支持多页面/多卡片轮播展示
- 提供开放的数据协议，方便接入自定义数据源
- 适配常见 ESP32 开发板与小尺寸屏幕

## 项目结构

```text
status-deck/
├── firmware/        # ESP32 固件代码
├── agent/           # 电脑端 Agent
├── docs/            # 协议、硬件、接线和开发文档
├── tools/           # 辅助脚本
└── README.md
```

> 当前仓库处于初始化阶段，目录会随着固件和 Agent 实现逐步补齐。

## 技术方向

- 设备端：ESP32、BLE、TFT/OLED/E-Paper 显示屏
- 电脑端：跨平台 Agent，负责采集数据并通过 BLE 下发
- 通信协议：轻量 JSON 或二进制帧，优先保证易调试和可扩展
- 开发方式：优先支持 PlatformIO，后续可补充 Arduino IDE / ESP-IDF 指南

## 快速开始

项目还在早期设计阶段，建议先按下面的方向准备环境：

1. 准备一块支持 BLE 的 ESP32 开发板
2. 准备一块小尺寸显示屏，例如 ST7789、SSD1306 或电子墨水屏
3. 安装 PlatformIO 或 ESP-IDF 开发环境
4. 运行电脑端 Agent，并在配置文件中启用需要展示的数据源
5. 将固件烧录到 ESP32 后，通过 BLE 与 Agent 配对

更完整的硬件清单、接线图、固件构建和 Agent 使用说明会在后续版本中补充。

## 数据源设想

- OpenAI / Anthropic / 其他 AI 服务用量
- API 额度、余额、速率限制和计费概览
- GitHub Actions、CI/CD、部署状态
- 服务器 ping、HTTP 健康检查、CPU、内存、磁盘
- 本机系统状态和开发环境状态
- 自定义 Webhook、脚本或插件数据

## 开发计划

- [ ] 定义 BLE 通信协议
- [ ] 搭建设备端固件工程
- [ ] 实现基础屏幕 UI
- [ ] 搭建桌面端 Agent
- [ ] 支持本机系统指标
- [ ] 支持 AI/API 用量数据源
- [ ] 编写硬件接线和外壳文档
- [ ] 发布第一版可复刻 Demo

## 贡献

欢迎提交 issue、讨论硬件方案、补充数据源或贡献代码。这个项目希望保持简单、透明和模块化，方便不同硬件、不同服务和不同桌面环境接入。

## License

计划以开源许可证发布。正式许可证文件会在项目进入首个可用版本前补充。
